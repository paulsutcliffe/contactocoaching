<%= form_for(@whatdowedo) do |f| %>
  <% if @whatdowedo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@whatdowedo.errors.count, "error") %> prohibited this whatdowedo from being saved:</h2>

      <ul>
      <% @whatdowedo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
